export type AddPostRequestType = {
  name: string;
  email: string;
  title: string;
  description: string;
  image: string;
  links: string;
};

export type AddReplyRequestType = {
  name: string;
  email: string;
  description: string;
};
